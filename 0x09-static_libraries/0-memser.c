#include "main.h"
/**
 * _memset - put particular value in memory
 * @s: first address to be occupied
 * @b: wanted value
 * @n: amount of bytes changable
 * Return: 0
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (0);
}
