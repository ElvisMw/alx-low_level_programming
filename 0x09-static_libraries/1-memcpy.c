#include "main.h"
/**
 * _memcpy - is a function that copies a memory area
 * @dest: storage place for memory
 * @src: memory location before its copied
 * @n: memory location after copying is complete
 * Return: new memory
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (dest);
}
